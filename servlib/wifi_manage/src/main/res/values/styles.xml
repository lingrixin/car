<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <style name="wifi_load">
        <item name="android:indeterminateDrawable">@anim/wifi_loading</item>
        <item name="android:minWidth">15dp</item>
        <item name="android:minHeight">15dp</item>
        <item name="android:maxWidth">30dp</item>
        <item name="android:maxHeight">30dp</item>
    </style>

    <style name="wifi_loading">
        <item name="android:indeterminateDrawable">@anim/wifi_progress_anim</item>
        <item name="android:minWidth">30dp</item>
        <item name="android:minHeight">30dp</item>
        <item name="android:maxWidth">50dp</item>
        <item name="android:maxHeight">50dp</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="wifi_connected_dialog">
        <item name="android:textColor">#333333</item>
        <item name="android:textSize">26px</item>
    </style>
    
    <style name="wifi_connected_content_line">
        <item name="android:paddingStart">40px</item>
        <item name="android:paddingEnd">36px</item>
    </style>

</resources>